info:
  name: invalid category
  type: http
  seq: 3

http:
  method: GET
  url: http://localhost:4000/api/tools?category=nothing
  params:
    - name: category
      value: nothing
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
