info:
  name: Make tool, but bad params
  type: http
  seq: 7

http:
  method: POST
  url: http://localhost:4000/api/tools
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
        "name": "",
        "category": "invalid",
        "condition": "invalid",
        "available": "yes",
        "maxLoanDays": -1
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
