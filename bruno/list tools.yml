info:
  name: list tools
  type: http
  seq: 1

http:
  method: GET
  url: http://localhost:4000/api/tools
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
